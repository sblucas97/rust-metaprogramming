[package]
name = "lib_core"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[lib]
name = "lib_core"
crate-type = ["rlib"]

[build-dependencies]
cc = "1.0"

[dependencies]
libloading = "0.7"
cudarc = { version = "0.19.3", features = ["cuda-version-from-build-system"] }
