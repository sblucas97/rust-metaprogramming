
    #include<stdio.h>
    #include<cuda_runtime.h>

    #define ROWS 10
    #define COLS 10
    
        int main() {
    fn my_kernel(x: f32) { println!("{}", x);
 }
        return 0;

        }
    